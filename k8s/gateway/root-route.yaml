apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: root-route
spec:
  parentRefs:
    - name: voting-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: vote
          port: 80
          